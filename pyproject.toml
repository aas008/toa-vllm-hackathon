[project]
name = "toa-vllm-hackathon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic[vertex]>=0.40.0",
    "pyyaml>=6.0",
    "paramiko>=3.0",
    "pandas>=2.0",
    "numpy>=1.24",
    "guidellm",
]
