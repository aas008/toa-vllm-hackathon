# Core dependencies
anthropic>=0.40.0       # Claude API client
pyyaml>=6.0             # YAML config parsing
paramiko>=3.0           # SSH client for remote GPU access

# Analysis dependencies
pandas>=2.0             # Data manipulation for benchmark results
numpy>=1.24             # Numerical operations

# Benchmark runner
guidellm
